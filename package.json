{
  "name": "tslint-no-unused-variable",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "dependencies": {
    "rimraf": "^2.6.2",
    "tslint": "^5.9.1",
    "typescript": "^2.8.1"
  },
  "scripts": {
    "clean": "rimraf *.js",
    "compile": "yarn clean && tsc",
    "lint": "tslint -p ./tsconfig.json -c ./tslint.json --fix"
  }
}
